# Current session

> Every agent starts here. This file holds the live state of the project *right now* and the next piece of work. Update it before ending a session (or at any significant milestone); move the previous state into [`handoffs.md`](./handoffs.md). Keep it short — details belong in the linked docs.

**Last updated:** 2026-09-02 ~20:40 (**#73 source-control epic complete and reviewed: all six parts shipped (#59a, #74, #75, #77, #78, #79, #81), two Opus reviews done, every blocking finding fixed (`d826298` host, `e6e9bbb` phone), leftovers filed as #82 #83; owner's phone install pending**; freeze lifted for source control only; the rest of the freeze stands)

## Next work

**Owner's phone install, then close the epic.** Install the build (below), confirm #74 #75 #77 #78 #79 #81 on the phone, close them and #73. Then the freeze is back on: #68/#69 health epics, with #82 (sweep `*.removing-*`) and #83 (review leftovers) as the first small items; #76 folds into #83.

**#73 review, what was fixed (2026-09-02 evening):** host — the removal preview *fails closed* (a git read that fails is a 503, never "nothing to lose"); a detached HEAD's orphan commits count as unpushed; a locked worktree is refused; deregistration is `worktree prune` (`remove --force` never worked after the rename); the branch is deleted only when merged or pushed by that very removal (a stale remote-tracking ref is not proof), or with `deleteBranch: true`; `closedAgents` counts agents; `worktree add` has a 120 s budget and rolls back the branch and folder on failure; the create roots check realpaths both sides; push always names `<remote> HEAD:refs/heads/<branch>` (a `push.default=matching` machine used to push every branch); pull-base aborts on any failure and says whether the tree was put back; stage runs at the top level; the PR badge cache forgets a branch the host just opened a PR for; route-level tests for 401/403/400/409. Phone — with uncommitted changes there is no amber on the removal sheet (both paths spelled out in the removed colour, plus "commit them first"); a line states the branch's fate; the host's receipt is shown with Done before the sheets close (one presentation transition at a time); every Source Control action flips its flag on the tap (no double commits/pushes/removals); the poll pauses while removing; an old host's 404 reads as "update the host"; link sends `number` or `url`; a retry after a failed agent start reuses the worktree it made; 13pt mono for shas and `+/−`. Seen live after the fixes: the dirty+unpushed removal sheet, the receipt, and the home without the card (`rm-00/01/02`).

**#81 as shipped (c04773e host, 72d98bf phone):** preview + confirmed removal (`GET /api/worktrees/removal`, `DELETE /api/worktrees`), agents' herdr tabs closed first, rename aside → `worktree remove --force` → background delete, branch kept unless merged / just pushed / confirmed away; `ProjectHistory.forget`. Phone: `···` → Remove → `RemoveWorktreeSheet` per the canvas (lines with `+a −d`, "no remote branch", the agent's state; amber "Push branch, then remove"; "Discard N changes and N commits" in the removed colour; "Keep it"). Seen live: a dirty, unpushed worktree with a shell agent showed all three lines; Discard closed the agent, removed the folder and branch, and the home lost the card (`rm-00`, `rm-01`). Not yet seen on a screen: push-then-remove and the clean/merged "Remove" state (host-tested). Lesson: an `accessibilityIdentifier` on a SwiftUI container overwrites every child's.

**#79 as shipped (5d08aac host, 7683c36 phone):** `gh.ts` finds `gh` on the login-shell PATH once (the card badge uses it too — the #76 leftover), `pull-requests.ts` read/create/link + `/api/repos/issues`; create pushes first (reuses `pushBranch`), `gh pr create --head --base` (`--fill` without a title). Phone: Pull request tab per the canvas; "From a GitHub issue" menu in the create sheet → `issue/<n>-<slug>`. Seen live: scratch PR #80 created from the phone (2 commits pushed first), tab showed `#80 · open · into main · 2 files +2 −0`; PR closed, branch deleted after. Not yet seen on a screen: the linked-PR path and the gh-logged-out card (host-tested only). Known: with several commits gh titles the PR after the branch (`demo/79 pr`) — a title field on the phone is a follow-up if the owner wants it.

**#78 as shipped (6339135 host, b0684b8 phone):** `GET /api/worktrees/log` (ahead/behind commits, upstream counts, push remote), `POST /api/worktrees/push` (upstream or `--set-upstream`, never force, refusals in words), `POST /api/worktrees/pull-base` (merge the local base; conflicts aborted and named; uncommitted work never overwritten). Commits tab: "N AHEAD OF MAIN · Push", "N BEHIND MAIN · Pull main in", receipt sentence under the list. Seen live: pull merged main in and the header went from `↑3 ↓1` to `↑4`; push from the launchd service reached GitHub (SSH batch mode, 90 s budget). Not yet seen on a screen: the `Pushed` / `No remote` label states (logic unit-tested only). Pull does **not** fetch first — it merges the local base as it stands (follow-up if the owner wants a fetch).

**Owner to do:** install the current build on the phone (device build with `-allowProvisioningUpdates`, separate `-derivedDataPath`, `xcrun devicectl device install app`) and confirm #74, #75, #77, #78, #79, #81 (close them and #73 after; the build is on the phone as of 20:45); one `npx tavi-host pair` on robin-PC for the preview door; decide when to publish the host (npm 0.1.14 has none of the source-control routes — robin-PC shows no worktree line until then).

**Follow-ups filed:** #83 (#73 review leftovers, host + phone; absorbs #76), #82 (sweep stranded `*.removing-*` folders), #72 (earlier git.ts follow-ups). Live tests that leave state behind: `testCreateWorktreeFromPicker` leaves a `ui-test/wt-…` worktree beside the chosen repo (remove by hand until part 6); `testCaptureSourceControl` commits for real in the worktree you point it at, and with `TEST_RUNNER_TAVI_AUDIT_PULL=1` / `TEST_RUNNER_TAVI_AUDIT_PUSH=1` also merges the base in / pushes to the remote; `TEST_RUNNER_TAVI_AUDIT_PR=1` opens a real pull request on GitHub (close it and delete the branch after). `testCaptureBranchFromIssue` creates nothing; `testCaptureRemoveWorktree` with `TEST_RUNNER_TAVI_AUDIT_REMOVE=1` really removes the worktree and its branch. Leftover `ui-test/wt-…` worktrees can now be removed from the phone (`···` → Remove). The worktree needs an agent in it for its repo card to appear on the home (a repo with no agent has no card).

**Connection drops, 2026-09-02 evening (owner-felt, not the git work):** measured from the Mac every 2 s — the host answered 90/90 probes in ≤30 ms while Tailscale pings to the phone timed out every ~15 s (avg 142 ms, max 928 ms), with Tavi closed as well as open; the Mac's WiFi is −48 dBm and idle, the router 4 ms. So the phone's WiFi hop stalls periodically. Shipped anyway: Offline only after two missed probes (5 s each, 1.5 s apart), no repos poll for an offline computer (`e4a78b9`); `/api/repos` served from cache in 30 ms, invalidated by every write, `?fresh=1` waits for git (`d69ebea`). robin-PC was offline all evening, so every 30 s the phone also dialled a dead peer — a difference from the morning worth remembering if this recurs.

**Rules relearned tonight:** look at every screen — three bugs were caught only by looking; the Mac *is* the host — no full simulator suites while the owner is on the phone (15-min load hit 16.5 and the phone sat on "Connecting…"); design before code, and put the design in the repo (§7.12, `docs/assets/source-control-canvas/`).

## Live state

- **Product:** Tavi by Farfield (Terminal Agent Vantage and Intervention). Repo `parvezrob/tavi`, checkout `~/Projects/tavi`. Host package `tavi-host` **0.1.14** on npm (#66 hand-back); **the checkout is ahead of it**: `GET /api/repos` (+`pullRequest`, `branches`), `POST /api/worktrees`, `/api/worktrees/{status,stage,unstage,commit,commit-message,log,push,pull-base,pull-request,pull-request/link,removal}`, `DELETE /api/worktrees`, `/api/repos/issues` — unpublished. Quick start `npx tavi-host pair`; after it, `tavi doctor` / `update` / `devices [revoke]` / `uninstall` work from any terminal.
- **Owner Mac:** host runs from the checkout as launchd `com.farfield.tavi.host` (deploy: `cd apps/host && npm run build && npm run service:install`; currently running tonight's build); preview door published (`:8443 → 127.0.0.1:8788`); herdr 0.8.2 under `brew services start herdr`; Claude hooks installed; the owner's Claude auto-allows Bash. `gh` is installed and logged in (the PR badge and, later, PR create use it).
- **Owner ubuntu (`robin-PC`):** paired; runs the published host (0.1.14). Needs one more `npx tavi-host pair` for the `tavi` command and the preview door.
- **Phone:** iPhone 12 Pro runs the **2026-09-02 20:45** build (`1bd81a2`: the whole #73 epic with the review fixes); free-Apple-ID profile expires **2026-09-08 13:01 UTC**. Both computers paired. Device builds go to `~/Library/Developer/Xcode/TaviDevice`.
- **Simulator:** iPhone 17 Pro `3CA94743-421A-4866-BD4F-2A92149AFE82`, paired to the Mac. `TAVI_DEV_HOST="<mac>,<mac>:443"` + `TAVI_DEV_HOST_NAMES` for a two-computer home. Audit captures under `TEST_RUNNER_TAVI_AUDIT=1` (`TaviScreenshotAudit`: home, changed files, preview, **Source Control** with `TEST_RUNNER_TAVI_AUDIT_WORKTREE`); export with `xcrun xcresulttool export attachments --path <xcresult> --output-path <dir>`.
- **Suites:** host 234 tests (`npm test`; `npm run check` is the type gate; `npm run build`); iOS 137 unit tests as xcresult counts them (`-only-testing:TaviTests`) + the live UI suite (needs `TEST_RUNNER_TAVI_DEV_HOST/TOKEN` exported; token: `npm run -s token` in apps/host; last full live run 2026-09-02 04:00, since then only targeted runs). Verification rule: for anything a person sees, look.
- **Host ↔ herdr:** protocol ≥ 17 + `agent.list` shape gate; `herdr agent attach <pane> --takeover`; herdr refuses `agent.send_keys` to a shell pane seconds old; pane size last-writer-wins, hand-back waits `DETACH_GRACE_MS` 8 s (#63); the events feed hands a Terminal back to herdr's detection when an agent starts in it (#66).
- **Phone ↔ host health:** `AgentDirectory.health` connecting/live/stale/offline/revoked; "Connecting…" bounded to 5 s then probe → Offline sticky; retries 2 → 30 s; `/api/repos` polled every 30 s (Equatable-gated).
- **Home (#74):** folder → worktrees → agents; a repository the host knows is one card, worktrees as glass groups (`TaviTheme.groupFill/groupHighlight`), agents beneath; a worktree with no agent still shows; "New worktree" last row → the New Agent sheet in worktree mode; a worktree header → `SourceControlSheet`.
- **Files (#25 #57 #61, PRD §7.10)** and **Preview (#58, §7.11)** unchanged; **Source Control (§7.12)** in progress as above.
- Open issues: #27–#29 (parked), #37 epic, #39 test seam, #49 WSL2 doc, #52 polish, #54 P3, **#73 epic (all parts shipped and reviewed)**, #74 #75 #77 #78 #79 #81 (close after the phone confirms), #82 sweep, #83 review leftovers (absorbs #76), #72; epics #68 #69 (health).
