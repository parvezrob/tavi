# Current session

> Every agent starts here. This file holds the live state of the project *right now* and the next piece of work. Update it before ending a session (or at any significant milestone); move the previous state into [`handoffs.md`](./handoffs.md). Keep it short — details belong in the linked docs.

**Last updated:** 2026-09-02 ~19:10 (session closed by the owner; **source-control epic #73 is three parts in — #59a, #74, #75, #77 shipped and live-tested on the simulator; owner's phone install pending**; freeze lifted for source control only; the rest of the freeze stands)

## Next work

**#73 part 4 — Commits.** Host: `GET /api/worktrees/log?path=…` (commits ahead of base and behind it, each `{sha, summary, author, when}`), `POST /api/worktrees/push` (first push sets the upstream via `push.autoSetupRemote`; say plainly when there is no remote), `POST /api/worktrees/pull-base` (merge the base in; refuse on conflict with a sentence, never leave a half-merge). Phone: the Commits tab in `SourceControlSheet` per the canvas — "3 ahead of main · Push", the list, "1 behind main · Pull main in". Same discipline as #77: fixed argv, path realpath'd then roots-checked, file its issue first, host gates → targeted phone tests → TaviTests once → a screenshot-audit run → look. Then **part 5** pull request (`gh pr view/create` + link; "from a GitHub issue" in the create sheet; say so when `gh` is missing/logged out) and **part 6** remove with the guardrail (`DELETE /api/worktrees/{id}`; refuse on uncommitted/unpushed unless the body confirms the counts; rename aside → `worktree remove` → background delete; branch deleted only when provably merged). Design and mechanics: `docs/PRD.md` §7.12, `docs/ORCA_SOURCE_CONTROL_RESEARCH.md`, canvas https://claude.ai/code/artifact/cf3e1af2-90ab-4c5b-9cec-5a68969538e7.

**Owner to do:** install the current build on the phone (device build with `-allowProvisioningUpdates`, separate `-derivedDataPath`, `xcrun devicectl device install app`) and confirm #74, #75, #77; one `npx tavi-host pair` on robin-PC for the preview door; decide when to publish the host (npm 0.1.14 has none of the source-control routes — robin-PC shows no worktree line until then).

**Follow-ups filed:** #76 (review leftovers: gh on the service PATH, `/api/repos` caching, chevron done, prunable done…), #72 (earlier git.ts follow-ups). Live tests that leave state behind: `testCreateWorktreeFromPicker` leaves a `ui-test/wt-…` worktree beside the chosen repo (remove by hand until part 6); `testCaptureSourceControl` commits for real in the worktree you point it at.

**Rules relearned tonight:** look at every screen — three bugs were caught only by looking; the Mac *is* the host — no full simulator suites while the owner is on the phone (15-min load hit 16.5 and the phone sat on "Connecting…"); design before code, and put the design in the repo (§7.12, `docs/assets/source-control-canvas/`).

## Live state

- **Product:** Tavi by Farfield (Terminal Agent Vantage and Intervention). Repo `parvezrob/tavi`, checkout `~/Projects/tavi`. Host package `tavi-host` **0.1.14** on npm (#66 hand-back); **the checkout is ahead of it**: `GET /api/repos` (+`pullRequest`, `branches`), `POST /api/worktrees`, `/api/worktrees/{status,stage,unstage,commit,commit-message}` — unpublished. Quick start `npx tavi-host pair`; after it, `tavi doctor` / `update` / `devices [revoke]` / `uninstall` work from any terminal.
- **Owner Mac:** host runs from the checkout as launchd `com.farfield.tavi.host` (deploy: `cd apps/host && npm run build && npm run service:install`; currently running tonight's build); preview door published (`:8443 → 127.0.0.1:8788`); herdr 0.8.2 under `brew services start herdr`; Claude hooks installed; the owner's Claude auto-allows Bash. `gh` is installed and logged in (the PR badge and, later, PR create use it).
- **Owner ubuntu (`robin-PC`):** paired; runs the published host (0.1.14). Needs one more `npx tavi-host pair` for the `tavi` command and the preview door.
- **Phone:** iPhone 12 Pro runs the **16:05** build (before #74/#75/#77); free-Apple-ID profile expires **2026-09-08**. Both computers paired.
- **Simulator:** iPhone 17 Pro `3CA94743-421A-4866-BD4F-2A92149AFE82`, paired to the Mac. `TAVI_DEV_HOST="<mac>,<mac>:443"` + `TAVI_DEV_HOST_NAMES` for a two-computer home. Audit captures under `TEST_RUNNER_TAVI_AUDIT=1` (`TaviScreenshotAudit`: home, changed files, preview, **Source Control** with `TEST_RUNNER_TAVI_AUDIT_WORKTREE`); export with `xcrun xcresulttool export attachments --path <xcresult> --output-path <dir>`.
- **Suites:** host 213 tests (`npm test`; `npm run check` is the type gate; `npm run build`); iOS 131 unit tests (`-only-testing:TaviTests`) + the live UI suite (needs `TEST_RUNNER_TAVI_DEV_HOST/TOKEN` exported; token: `npm run -s token` in apps/host; last full live run 2026-09-02 04:00, since then only targeted runs). Verification rule: for anything a person sees, look.
- **Host ↔ herdr:** protocol ≥ 17 + `agent.list` shape gate; `herdr agent attach <pane> --takeover`; herdr refuses `agent.send_keys` to a shell pane seconds old; pane size last-writer-wins, hand-back waits `DETACH_GRACE_MS` 8 s (#63); the events feed hands a Terminal back to herdr's detection when an agent starts in it (#66).
- **Phone ↔ host health:** `AgentDirectory.health` connecting/live/stale/offline/revoked; "Connecting…" bounded to 5 s then probe → Offline sticky; retries 2 → 30 s; `/api/repos` polled every 30 s (Equatable-gated).
- **Home (#74):** folder → worktrees → agents; a repository the host knows is one card, worktrees as glass groups (`TaviTheme.groupFill/groupHighlight`), agents beneath; a worktree with no agent still shows; "New worktree" last row → the New Agent sheet in worktree mode; a worktree header → `SourceControlSheet`.
- **Files (#25 #57 #61, PRD §7.10)** and **Preview (#58, §7.11)** unchanged; **Source Control (§7.12)** in progress as above.
- Open issues: #27–#29 (parked), #37 epic, #39 test seam, #49 WSL2 doc, #52 polish, #54 P3, **#73 epic (parts 4–6)**, #72 + #76 follow-ups; epics #68 #69 (health).
