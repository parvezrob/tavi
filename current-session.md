# Current session

> Every agent starts here. This file holds the live state of the project *right now* and the next piece of work. Update it before ending a session (or at any significant milestone); move the previous state into [`handoffs.md`](./handoffs.md). Keep it short — details belong in the linked docs.

**Last updated:** 2026-09-02 ~16:45 (session closed by the owner; **#58 #66 #67 #70 #71 all closed today**, each proven live and confirmed on the phone; host 0.1.14 published; phone runs the 16:05 build; **next session starts #59 worktrees**; feature freeze; epics #68 #69)

## Next work

**#58 is closed (2026-09-02 12:05).** Shipped in host 0.1.13 + the 05:10 phone build; every durability number is on the issue: 2-hour host soak flat; preview sheet flat over 200 open/close cycles; preview open 60 min at 80 MB flat with 30 hot-reload edits through the door; terminal open 60 min streaming 5 lines/s at 77 → 79 MB; `leaks` 0 and single-digit heap counts everywhere. One real leak fixed on the way (4dd2acc: the home rebuilt every computer's directory per return from a terminal). No home "dev server" badge (decided on the issue: the transcript signal is not reliable, see #71). **Owner still to do:** one `npx tavi-host pair` on robin-PC for the door there. Memory tooling for later health work: `TaviUITests/TaviMemoryChecks` + `scripts/memsample.sh` (docs/DEVELOPMENT.md).

**#70 and #71 are closed (2026-09-02 15:10, owner asked "fix #70 and #71").** #70: the app's WebSockets (terminal + events stream) run on Network.framework — `Transport/NetworkWebSocketTask.swift` behind the existing `makeSocket` seam; `URLSessionWebSocketTask` is gone. 200 terminal trips: delta per 40 trips 1.6 MB → noise around 0, heap 3 `nw_connection` / 20 certificates (was 243 / 2 916), leaks 0. Network.framework hides the HTTP status of a rejected upgrade, so a rejection is classified by `GET /api/agents` (`TerminalHandshakeProbe`: 401 revoked, pane missing gone, else retry). #71: the accessible transcript is read from Ghostty's grid (`GhosttyScreenText.activeArea`, active area only, 250 ms cadence, published on change); `TerminalAccessibleTranscript` deleted; unit test with a real surface + the stay's row-text assertion restored and passed live (3 min). Both on the issues with tables. **Phone build installed 15:20** (both fixes on the iPhone). Live-suite lesson today: ask whether the owner is in the app *before* starting a simulator run against the host (one run was aborted for this).

**#66 and #67 are closed (2026-09-02 16:00).** #66 (host, 472486c): the events feed hands a Tavi-reported Terminal back to herdr's detection (`pane.release_agent`) once `agent_session.agent` names an agent, carries the row through herdr's ~2 s unlisted gap, re-reports `shell` when the agent exits (pane still exists), maps a shell's `done` → `idle`; the terminal upgrade retries the pane lookup 3 s. Proven through `/api/agents` (Terminal → claude idle/working/done → Terminal). **Host 0.1.14 published 16:25 (the owner's npm login had expired: `npm publish` says 404, `npm whoami` says 401 — log in again); robin-PC picks it up on its daily self-update or with `tavi update` there.** #67 (phone, 4a9712a): Create opens the new pane's terminal from the sheet's `onDismiss`; both picker live tests updated and green, screenshots looked at. Phone build 16:05 installed. Seen while looking: a Claude agent's identity chip reads "· tavi claude" (empty name before the dot) during the seconds before detection names it — cosmetic, not filed (freeze; note for #52/#54 polish if the owner wants it).

**Next session: #59 (owner, 2026-09-02 16:45).** Git worktrees — create from the phone, show which agent lives in which tree. The issue carries the plan; sequence it as written: **(a)** read-only worktree/branch visibility in the project group (host `git.ts`, shell-outs only: `worktree list --porcelain`, `status --porcelain=v2`, `rev-list --left-right --count`; `GET /api/repos`; home grouping computer → repo → worktree → agents, a list not a tree, row like `fix/foo · +3/−1 · 2 dirty · ⬤ Claude Code waiting`), **(b)** create-worktree as the third answer to *where* in the New Agent sheet (`POST /api/worktrees`, #24 roots guardrail + `outsideRoots` confirm; then start the agent there — #67's open-on-create carries through), **(c)** remove with guardrails (`DELETE /api/worktrees/{id}`; clean tree or an explicit "discard N changes" confirm, never a silent `--force`). Out of scope: merge, rebase, PRs. Verify each part live on the simulator (the Mac is the host; ask whether the owner is in the app first) and look at every screen. Also on the way: Files shows uncommitted work only — decide where "changes since branching from main" lands (#59's own note).

**Was: the owner's two (2026-09-02, filed with root causes and fixes proven live):** **#66** a Terminal pane that starts `claude` stays "Terminal" — call `pane.clear_agent_authority` on `pane.agent_detected` (host only); **#67** creating an agent should open its terminal — pass the returned paneId from `POST /api/herdr/tabs` to `openAgent` (phone only). Both are small; do them before #59.

**After #58:** #59 worktrees (create from the phone; which agent lives in which tree; also where "changes since branching from main" would land — Files shows uncommitted work only). #54 P3 brand moments whenever a light session fits. #37 App Store epic gates any submission. #39 test seam: the two Claude-permission live tests (`testNeedsYouSurvivesVisitingTheBlockedAgent`, `testAnswerWaitingPermissionFromNeedsYouCard`) skip whenever the owner's Claude auto-allows Bash — stage them through `POST /api/hooks/claude` to make them deterministic.

**Design pages from this stretch (for reference):** home v3 before/after https://claude.ai/code/artifact/2a2d609e-d2bd-418e-b55a-b7621823a098 · the three calls (all decided per the leans) https://claude.ai/code/artifact/c7b31825-b308-4c49-9301-fa4f8bffd3cc.

**Working rules learned tonight (also in AGENTS.md / docs):** the owner's Mac *is* the host — no full suites or device builds on it while the owner is on the phone; keep the host quiet during live suites (a mid-suite redeploy skipped two tests). Say `npx tavi-host <cmd>` in docs for npx installs unless the `tavi` shim is known to be there; the bare `npx tavi` is a stranger's package.

**Publishing rule (owner + agent, 2026-09-01):** the agent bumps `apps/host/package.json` + `src/config.ts` `VERSION` only when it says a release is worth publishing; the owner runs `cd apps/host && npm publish` in Terminal.app (2FA; the auth URL is masked under Claude Code). Versions are immutable ("cannot publish over the previously published versions" = already up), propagate in 1–2 min, and every publish restarts every paired host.

**Feature freeze (owner, 2026-09-02):** the open issues as of tonight (#58 #66 #67 #59 #52 #54 #49 #39 #37, parked #27–#29) are the whole remaining feature set. After them, **no new features**: performance, optimization, and health only (memory/leaks on the phone incl. the Ghostty surface, host soak/load, reconnect, battery, startup, test determinism). Lifting the freeze is the owner's explicit call; agents do not file feature issues. Written into `docs/ROADMAP.md`. **The post-freeze work is #68 and #69** (#70 and #71 done 2026-09-02) (#69 = code health to 9/10: route split, AgentDirectory split, one HTTP client, lint/format + phone CI, fixtures once, herdr contract test, docs map) (performance/optimization/health epic: baseline numbers, 14 host and 13 phone findings ranked with file:line, measure-first plan, suggested order).

**Open owner decisions:** whether phones should stop accepting the shared host token; a hosted relay opt-in for people who bounce off Tailscale (docs/PRODUCT.md principle 2 says direct only; decide after testers); GitHub Actions publish job.

## Live state

- **Product:** Tavi by Farfield (Terminal Agent Vantage and Intervention). Repo `parvezrob/tavi`, checkout `~/Projects/tavi`. Host package `tavi-host` **0.1.14** on npm = the checkout (#66 hand-back) (Apache-2.0; everything else © Farfield). Quick start `npx tavi-host pair`; after it, `tavi doctor` / `update` / `devices [revoke]` / `uninstall` work from any terminal (the shim `pair` adds, #64).
- **Owner Mac:** host runs from the checkout as launchd `com.farfield.tavi.host` (deploy: `cd apps/host && npm run build && npm run service:install`, then `/api/health` → 0.1.14 now); the preview door is published (`tailscale serve status` shows `:8443 → 127.0.0.1:8788`, `doctor` ✓ Preview door); herdr 0.8.2 under `brew services start herdr` (headless); Claude hooks installed; the owner's Claude currently auto-allows Bash (permission dialogs are rare).
- **Owner ubuntu (`robin-PC`):** paired; runs the published host (self-updates daily; 0.1.14 due within a day, or `tavi update`). Needs one more `npx tavi-host pair` to get the `tavi` command **and the preview door** (until then Preview there says the door is not set up, with that instruction).
- **Phone:** iPhone 12 Pro runs the `16:05` build (home v3, Files, Preview, Network.framework sockets + grid transcript, **create opens the terminal**); free-Apple-ID profile expires **2026-09-08** — rebuild with `-allowProvisioningUpdates` into `-derivedDataPath` separate from the simulator's and `xcrun devicectl device install app`. Both computers paired.
- **Simulator:** iPhone 17 Pro `3CA94743-421A-4866-BD4F-2A92149AFE82`. Two-computer home without a second machine: `TAVI_DEV_HOST="<mac>,<mac>:443"` + `TAVI_DEV_HOST_NAMES="MacBook Air,robin-PC"`; audit captures under `TEST_RUNNER_TAVI_AUDIT=1` (`TaviScreenshotAudit`, incl. `testCaptureChangedFilesAndDiff` with `TEST_RUNNER_TAVI_AUDIT_CWD`).
- **Suites:** host 194 tests (`npm test`, `npm run check` is the type gate, `npm run build`); iOS 124 unit tests + live UI suite (last full run 2026-09-02 04:00: 131 passed, 6 skips explained; since then only `TaviScreenshotAudit.testCapturePreviewDevServer` was added and run alone — needs `TEST_RUNNER_TAVI_AUDIT_PREVIEW_CWD=/Users/parvezrobin/Projects/preview-demo` with Vite running there). Live UI tests need `TEST_RUNNER_TAVI_DEV_HOST/TOKEN` exported (token: `npm run -s token` in apps/host). Verification rule: for anything a person sees, look.
- **Host ↔ herdr:** protocol ≥ 17 + `agent.list` shape gate; `herdr agent attach <pane> --takeover` always; herdr refuses `agent.send_keys` to a shell pane seconds old (tests type through the app's surface); pane size is last-writer-wins, hand-back to the desktop waits `DETACH_GRACE_MS` 8 s (#63).
- **Phone ↔ host health:** `AgentDirectory.health` connecting/live/stale/offline/revoked; "Connecting…" bounded to 5 s then probe → Offline sticky across foregrounds; retries back off 2 → 30 s.
- **Preview (#58, PRD §7.11):** door `https://<name>.ts.net:8443` → `127.0.0.1:8788`; `/api/preview/{door,candidates,stop}`, `POST /api/preview`, `/api/preview/{id}[/keepalive]`; tickets in memory, 120 s grace; Vite binds `[::1]` on macOS so the host probes both loopbacks.
- **Files (#25 #57 #61, PRD §7.10):** `/api/changes`, `/api/changes/file`, `/api/files{,/stat,/content,/raw}` — realpath **then** roots, secrets by name; phone `Features/Files/`.
- Open issues: #27–#29 (parked), #37 epic (now also carries the outside-tester acceptance run from #47), #39 test seam, #49 WSL2 doc, #52 polish, #54 P3, #59 worktrees; epics #68 #69 (health).
