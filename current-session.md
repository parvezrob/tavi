# Current session

> Every agent starts here. This file holds the live state of the project *right now* and the next piece of work. Update it before ending a session (or at any significant milestone); move the previous state into [`handoffs.md`](./handoffs.md). Keep it short — details belong in the linked docs.

**Last updated:** 2026-09-04 ~00:40 (session closed; **#69 closed — all six packages on main** (#89 #90 #91 #92 #94 #95), CI green; phone still runs the #54 build 29327dc (the #69 phone work is not on the phone yet — install over USB before judging it); the Mac's service is still 14c75a4 = 0.1.17, behaviour-identical to `main`'s host, nothing published; **next: #68 measurement harness (host `npm run bench`, phone XCTMemoryMetric/XCTCPUMetric), then host findings 1, 2, 8; #37 on the owner's side**)

## Next work

**1. #68 performance — START HERE, with the sub-agent loop in AGENTS.md.** Step one is the measurement harness (the epic's rule: a number before and after): host `npm run bench` (events feed + one terminal at a fixed output rate for 10 min → CPU %, RSS, FDs, event-loop lag p99, bytes per phone) and phone `XCTMemoryMetric`/`XCTCPUMetric` tests (home with 2 hosts × 8 agents flapping; terminal under a 1 MB/min storm; Files on a 500-file folder; preview open/close ×200). Then host findings 1, 2, 8 (the 2 s sync file read + hash per socket; the snapshot stringified per client; no server heartbeat) as one package with the bench numbers; phone: the `navigationDestination` re-creation finding recorded on #68 (2026-09-03). Phone finding 3 was amended on #68 — do not reintroduce a signature skip on previews. Measurement soaks load the Mac: only when the owner is not on the phone.

**Install the #69 build on the phone** (cable only, rules below) before judging the terminal or the home: it carries the owned-type splits and the redraw fixes; the ctrl cap, Jump-to, rename, needs-you, and a working agent's live excerpt are the five things to look at. `testNeedsYouSurvivesVisitingTheBlockedAgent` skipped tonight (its disposable claude never reached blocked) — re-run once a blocked agent exists.

**Cold audit 2026-09-04 → #96 (code health second pass):** host 7.5, phone 6.5 — not the 9 #69 claimed; #69's close comment carries the correction. Highest value first: make the lint gates able to go red (phone `--strict` + baseline, host line-count gate, branch protection), fold `HostRoutes`/`HostConnection`/`HostPairing` onto `HostClient` (14 hand-rolled requests), stop the host lying on failed reads (`git.ts:540` → "in sync"), unit-test the home networking stack, refresh the DEVELOPMENT.md phone map for #95's types. Weave #96 items into #68 packages where they touch the same files.

**#69 leftovers (folded into #96):** item 15 (delete the terminal v1 bridge; owner call once every phone has spoken v2 for a week); files still over 400 lines — host `bootstrap.ts` 852, `worktrees.ts` 678, `herdr.ts` 663, `source-control.ts` 659, `preview.ts` 594, `git.ts` 558; phone `SessionsView` 839, `NewAgentSheet` 800, `SessionsHomeComponents` 659. #93 (Files too-old sentence unreachable) is a small real bug.

**2. #37 App Store readiness (owner-driven, in parallel).** The paid developer account (ends the 7-day profile), the privacy strings (camera, Face ID, microphone, speech are already in the target's Info keys — check them against what the app does), the App Store screenshots (the #54 close-out left the audit captures as the raw material), then the TestFlight build itself. The app icon and dark launch screen are in (PRD §7.15).

**The #54 build is on the phone** (installed over USB 2026-09-03 ~19:50, profile to 2026-09-08 13:01 UTC): icon, launch screen, partly-staged dash, shared 12 pt headers, New agent on the idle card — the idle card is the one screen not yet looked at live (the Mac always has agents; look once with every tab closed).

**Owner to do:** `npx tavi-host@latest pair` on the Mac to put the managed runtime (0.1.17) back in place of the checkout; robin-PC `tavi update` + one `npx tavi-host pair` for the preview door; look at the PR title field and Pull main in once on the #83 build; Settings → robin-PC (offline) should offer "Forget on this iPhone only" at once; on the Archer C6, check AP isolation is off and both devices share a band.

**Parked until after TestFlight:** #68 #69 (health), #39, #49, #27–#29; #88 extras (camera, paste, thumbnail chip); the #83 leftovers named in its close comment.

**Rules for anyone touching the phone:** cable only (`xcrun devicectl device info details --device 9B6F918E-EED4-59EA-8BF3-0ED972C01B93 | grep transportType` must say `wired`; the first read after a cable-in can say `localNetwork` while CoreDevice still holds its wireless path — kill the daemons and read again), then quit Xcode and `pkill -f remotepairingd; pkill -f CoreDeviceService` — every `devicectl` call and a running Xcode re-open the wireless tunnel that put the phone's radio in the 09-02 state. Nothing heavy on the Mac while the owner is on the phone. When the owner reports a connection problem: `second-opinion` + Opus cold seats *before* arguing; measure the phone's side (Safari to `/api/host`, `tailscale ping` loop with Tavi force-quit, `tailscaled.log` at `/opt/homebrew/var/log/`) or say you cannot.

**Rules relearned 09-02/03:** the Mac *is* the host — nothing heavy on it while the owner tests, and phone installs over USB only; when the owner says "it worked this morning", A/B the old build first; look at every screen; design before code and keep the design in the repo (§7.12); close issues, don't pile them up — one close comment names what was left on purpose.

## Live state

- **Product:** Tavi by Farfield (Terminal Agent Vantage and Intervention). Repo `parvezrob/tavi`, checkout `~/Projects/tavi`. Host package `tavi-host` **0.1.17** on npm (published 2026-09-03 ~03:10) = the checkout at `14c75a4` (everything: `/api/repos` cached, `POST /api/worktrees`, `/api/worktrees/{status,stage,unstage,commit,commit-message,log,push,pull-base,pull-request,pull-request/link,removal}`, `DELETE /api/worktrees` with `unlock`, `/api/repos/issues`). Quick start `npx tavi-host pair`; after it, `tavi doctor` / `update` / `devices [revoke]` / `uninstall` work from any terminal.
- **Owner Mac:** the host runs the **checkout at 14c75a4** (= the published 0.1.17) as launchd `com.farfield.tavi.host` since 2026-09-03 ~02:45. To run the checkout instead: `cd apps/host && npm run build && npm run service:install` (that replaces the managed runtime; `npx tavi-host@latest pair` puts it back). Preview door published (`:8443 → 127.0.0.1:8788`); herdr 0.8.2; Claude hooks installed; the owner's Claude auto-allows Bash. `gh` is installed and logged in; the launchd service finds it on the login-shell PATH.
- **Owner ubuntu (`robin-PC`):** paired; was offline all evening; runs 0.1.14 until `tavi update`.
- **Phone:** iPhone 12 Pro runs the **#54** build (`29327dc`) since 2026-09-03 ~19:50, installed over USB (previous: #83 `5d59dd4`); Private Wi-Fi Address is off (LAN address now `192.168.0.228`), Cellular Data was toggled during testing. The rollback tag `ios-known-good-pre-59` (`3ecef75`) stays. A cable that only charges leaves CoreDevice on `localNetwork`; the second cable/port showed `wired`. Xcode is quit and the wireless daemons killed. Free-Apple-ID profile expires **2026-09-08 13:01 UTC**. Device builds go to `~/Library/Developer/Xcode/TaviDevice` (rollback: `…/TaviRollback`; the Ghostty xcframework is untracked and must be linked into a rollback worktree). Both computers paired.
- **Simulator:** iPhone 17 Pro `3CA94743-421A-4866-BD4F-2A92149AFE82`, paired to the Mac (shut down tonight). Audit captures under `TEST_RUNNER_TAVI_AUDIT=1` (`TaviScreenshotAudit`: home, changed files, preview, Source Control with `TEST_RUNNER_TAVI_AUDIT_WORKTREE`, plus opt-in `_PULL`/`_PUSH`/`_PR`/`_REMOVE` that act for real; `testCaptureBranchFromIssue` creates nothing). Export with `xcrun xcresulttool export attachments --path <xcresult> --output-path <dir>`. A worktree needs an agent in it for its repo card to appear on the home.
- **Suites:** host 251 tests + 1 skipped contract test (`npm test`; `TAVI_LIVE_HERDR=1` runs it against the Mac's herdr; `npm run lint` = biome; `npm run check` is the type gate; `npm run build`); iOS `apps/ios/scripts/check.sh` = `lint.sh` (swiftformat + swiftlint, 70 warnings recorded, 0 errors) → simulator build → 146 unit tests as xcresult counts them (`-only-testing:TaviTests`) + the live UI suite (needs `TEST_RUNNER_TAVI_DEV_HOST/TOKEN` exported; token: `npm run -s token` in apps/host; `testCreateWorktreeFromPicker` leaves a `ui-test/wt-…` worktree — remove it from the phone via `···`). CI green on `main`. Verification rule: for anything a person sees, look.
- **Host ↔ herdr:** protocol ≥ 17 + `agent.list` shape gate; `herdr agent attach <pane> --takeover`; herdr refuses `agent.send_keys` to a shell pane seconds old; pane size last-writer-wins, hand-back waits `DETACH_GRACE_MS` 8 s (#63); the events feed hands a Terminal back to herdr's detection when an agent starts in it (#66).
- **Phone ↔ host health:** `AgentDirectory.health` connecting/live/stale/offline/revoked; "Connecting…" bounded to 5 s then a probe (5 s, two misses 1.5 s apart) → Offline, which a live snapshot clears; on a stream drop the redial starts at once (backoff 2 → 30 s) and the probe runs beside it, never in front (2ac15a8); `/api/repos` polled every ~30 s (±20 % jitter) only while the computer is not offline and the stream is not stale (Equatable-gated; kept across restarts). The events socket has no keepalive of its own (#86).
- **Home (#74):** folder → worktrees → agents; a repository the host knows is one card, worktrees as glass groups (`TaviTheme.groupFill/groupHighlight`), agents beneath; a worktree with no agent still shows; "New worktree" last row → the New Agent sheet in worktree mode; a worktree header → `SourceControlSheet` (Changes / Pull request / Commits; `···` → Start an agent here, Remove worktree).
- **Files (#25 #57 #61, PRD §7.10)**, **Preview (#58, §7.11)** unchanged; **Source Control (§7.12)** complete; **design system (§7.15)** closed: `TaviTheme` is the one source of colour and radius, `SectionHeader` the one section register (12 pt small caps, trailing action slot), icon = terminal caret + amber cursor (`docs/assets/app-icon/`, rendered by `scripts/render-app-icon.swift`), launch screen = canvas colour via the merged `Tavi/Info.plist`.
- Open issues: **#68** (perf; measurement first), **#37** → TestFlight, #93 (small bug). Closed 2026-09-03/04: #69 with #89 #90 #91 #92 #94 #95; #54 #52; #86 #84 #88 #83. Parked: #39 #49 #27–#29.
