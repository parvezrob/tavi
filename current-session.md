# Current session

> Every agent starts here. This file holds the live state of the project *right now* and the next piece of work. Update it before ending a session (or at any significant milestone); move the previous state into [`handoffs.md`](./handoffs.md). Keep it short — details belong in the linked docs.

**Last updated:** 2026-09-03 ~02:30 (**#83 git leftovers done and closed** — host 14c75a4, phone 5d59dd4, both suites green, **not yet installed on the phone and the Mac's service still runs the pre-#83 checkout**; #86 and #88 are on the phone since 651e383; **next work is #54**)

## Next work

**1. #73 is closed** (23:58: the owner exercised worktree-from-issue, PR create/close and remove live from the phone on cellular; #74 #75 #77 #78 #79 #81 closed with it). Still to look at once: Settings → robin-PC (offline) should offer "Forget on this iPhone only" at once. robin-PC: `tavi update` to 0.1.16 plus one `npx tavi-host pair` for the preview door.

**The road to TestFlight (owner, 2026-09-03 00:05: close issues, don't pile up). Four issues, in this order, then ship:**

**2. #86 connection resilience** (with #84 inside it): one shared `URLSession`, no terminal teardown on `NWPathMonitor` snapshots, one reconnect coordinator per host, single-flight probes, events heartbeat, jitter, the connection in plain words, cellular-only release check. Read the night entry in `handoffs.md` and the three reviews' items in #86 before designing; design in the repo first (§7.12 precedent).

**2b. #88 attach an image — built, on the phone.** Paperclip on the composer → photo library → 2048 px JPEG → `POST /api/files/upload?cwd=` → `<cwd>/.tavi/uploads/…` (out of git via `.git/info/exclude`, swept after 7 days) → the path appended to the message + a note saying where it went (PRD §7.14). The owner: "passes the test" (01:10). **To finish:** close #88. Camera, paste, and a thumbnail chip are later. Note for the record: the picker is Apple's PhotosPicker — no permission prompt by design; the image is read from the Mac, never the phone, once sent.

**3. #83 git leftovers — done, closed.** Host (14c75a4): `*.removing-*` folders swept on start and hourly, kept out of the picker, named by `tavi doctor` with the `rm` command (#82); `gh pr list` four at a time per repo with the losers killed at the 3 s budget (#85); `worktree list --porcelain -z`; `truncated` per repo and on the answer; `{ repos: [], error }` when git is missing; one `for-each-ref --format=%(ahead-behind:)` per repo (detached measured by HEAD, remote default when no local copy); `withinRoots` per worktree; one `aheadBehind`; gh through `runGh`; **pull-base fetches the base's upstream first** and answers `fetched`/`from`; the removal preview names tab-mates outside the worktree (`alsoClosed`). Phone (5d59dd4): PR title field prefilled from the newest commit; tab-mates on the removal sheet; "as of the computer's last fetch"; grouping guards; repos cleared after three straight failures; "detached · a1b2c3d". **To finish:** install the phone build (cable only) and `cd apps/host && npm run build && npm run service:install` so the Mac runs it; then the owner looks at the PR title field and Pull main in once. **Left on purpose** (noted in the #83 close comment, no new issue): the 409 removal body's `preview` is re-read rather than decoded (one extra round trip); half-staged checkbox, header 13 pt, group highlight mask → #54; the sheet header's agent line is frozen at tap time; no audit capture of push-then-remove; `scanWorkspaces` one level deep; `origin` hardcoded in `findDefaultBranch`.

**4. #54 design pass — START HERE** (absorb #52; take the three visual notes from #83 above), then **#37 App Store readiness** (the paid developer account ends the 7-day profile; privacy strings; the TestFlight build itself).

**Parked until after TestFlight:** #68 #69 (health), #39, #49, #27–#29.

**Rules for anyone touching the phone:** cable only (`xcrun devicectl device info details --device 9B6F918E-EED4-59EA-8BF3-0ED972C01B93 | grep transportType` must say `wired`), then quit Xcode and `pkill -f remotepairingd; pkill -f CoreDeviceService` — every `devicectl` call and a running Xcode re-open the wireless tunnel that put the phone's radio in tonight's state. Nothing heavy on the Mac while the owner is on the phone. When the owner reports a connection problem: `second-opinion` + Opus cold seats *before* arguing; measure the phone's side (Safari to `/api/host`, `tailscale ping` loop with Tavi force-quit, `tailscaled.log` at `/opt/homebrew/var/log/`) or say you cannot.

**Owner to do:** longer stretch on the fixed build; look at the robin-PC unpair screen; robin-PC update + pair; on the Archer C6, check AP isolation is off and both devices share a band.

**Rules relearned tonight:** the Mac *is* the host — nothing heavy on it while the owner tests, and phone installs over USB only; when the owner says "it worked this morning", A/B the old build first; look at every screen (the removal sheet's amber, the container-identifier trap, the menu row's hit area were all caught by looking); design before code and keep the design in the repo (§7.12).

## Live state

- **Product:** Tavi by Farfield (Terminal Agent Vantage and Intervention). Repo `parvezrob/tavi`, checkout `~/Projects/tavi`. Host package `tavi-host` **0.1.16** on npm = the checkout at `84bd57f` (everything: `/api/repos` cached, `POST /api/worktrees`, `/api/worktrees/{status,stage,unstage,commit,commit-message,log,push,pull-base,pull-request,pull-request/link,removal}`, `DELETE /api/worktrees` with `unlock`, `/api/repos/issues`). Quick start `npx tavi-host pair`; after it, `tavi doctor` / `update` / `devices [revoke]` / `uninstall` work from any terminal.
- **Owner Mac:** the host now runs the **managed runtime** (re-paired with `npx tavi-host pair` tonight), launchd `com.farfield.tavi.host`, 0.1.16. To run the checkout instead: `cd apps/host && npm run build && npm run service:install` (that replaces the managed runtime; `npx tavi-host@latest pair` puts it back). Preview door published (`:8443 → 127.0.0.1:8788`); herdr 0.8.2; Claude hooks installed; the owner's Claude auto-allows Bash. `gh` is installed and logged in; the launchd service finds it on the login-shell PATH.
- **Owner ubuntu (`robin-PC`):** paired; was offline all evening; runs 0.1.14 until `tavi update`.
- **Phone:** iPhone 12 Pro runs the **fixed epic** build (`2ac15a8`) since 23:27, installed over USB; Private Wi-Fi Address is off (LAN address now `192.168.0.228`), Cellular Data was toggled during testing. The rollback tag `ios-known-good-pre-59` (`3ecef75`) stays. A cable that only charges leaves CoreDevice on `localNetwork`; the second cable/port showed `wired`. Xcode is quit and the wireless daemons killed. Free-Apple-ID profile expires **2026-09-08 13:01 UTC**. Device builds go to `~/Library/Developer/Xcode/TaviDevice` (rollback: `…/TaviRollback`; the Ghostty xcframework is untracked and must be linked into a rollback worktree). Both computers paired.
- **Simulator:** iPhone 17 Pro `3CA94743-421A-4866-BD4F-2A92149AFE82`, paired to the Mac (shut down tonight). Audit captures under `TEST_RUNNER_TAVI_AUDIT=1` (`TaviScreenshotAudit`: home, changed files, preview, Source Control with `TEST_RUNNER_TAVI_AUDIT_WORKTREE`, plus opt-in `_PULL`/`_PUSH`/`_PR`/`_REMOVE` that act for real; `testCaptureBranchFromIssue` creates nothing). Export with `xcrun xcresulttool export attachments --path <xcresult> --output-path <dir>`. A worktree needs an agent in it for its repo card to appear on the home.
- **Suites:** host 251 tests (`npm test`; `npm run check` is the type gate; `npm run build`); iOS 146 unit tests as xcresult counts them (`-only-testing:TaviTests`) + the live UI suite (needs `TEST_RUNNER_TAVI_DEV_HOST/TOKEN` exported; token: `npm run -s token` in apps/host; `testCreateWorktreeFromPicker` leaves a `ui-test/wt-…` worktree — remove it from the phone via `···`). CI green on `main`. Verification rule: for anything a person sees, look.
- **Host ↔ herdr:** protocol ≥ 17 + `agent.list` shape gate; `herdr agent attach <pane> --takeover`; herdr refuses `agent.send_keys` to a shell pane seconds old; pane size last-writer-wins, hand-back waits `DETACH_GRACE_MS` 8 s (#63); the events feed hands a Terminal back to herdr's detection when an agent starts in it (#66).
- **Phone ↔ host health:** `AgentDirectory.health` connecting/live/stale/offline/revoked; "Connecting…" bounded to 5 s then a probe (5 s, two misses 1.5 s apart) → Offline, which a live snapshot clears; on a stream drop the redial starts at once (backoff 2 → 30 s) and the probe runs beside it, never in front (2ac15a8); `/api/repos` polled every ~30 s (±20 % jitter) only while the computer is not offline and the stream is not stale (Equatable-gated; kept across restarts). The events socket has no keepalive of its own (#86).
- **Home (#74):** folder → worktrees → agents; a repository the host knows is one card, worktrees as glass groups (`TaviTheme.groupFill/groupHighlight`), agents beneath; a worktree with no agent still shows; "New worktree" last row → the New Agent sheet in worktree mode; a worktree header → `SourceControlSheet` (Changes / Pull request / Commits; `···` → Start an agent here, Remove worktree).
- **Files (#25 #57 #61, PRD §7.10)**, **Preview (#58, §7.11)** unchanged; **Source Control (§7.12)** complete.
- Open issues, in closing order: **#86** (+#84, awaiting the owner's confirmation) → **#54** (+#52) → **#37** → TestFlight. #83 (held #72 #76 #82 #85) closed 2026-09-03. Parked: #68 #69 #39 #49 #27–#29.
