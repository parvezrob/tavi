diff --git a/src/build/LibtoolStep.zig b/src/build/LibtoolStep.zig
index 856a33aa8..4dd71622c 100644
--- a/src/build/LibtoolStep.zig
+++ b/src/build/LibtoolStep.zig
@@ -34,0 +35,5 @@ pub fn create(b: *std.Build, opts: Options) *LibtoolStep {
+    run_step.setEnvironmentVariable(
+        "DEVELOPER_DIR",
+        std.posix.getenv("MOCHA_XCODE_DEVELOPER_DIR") orelse
+            @panic("MOCHA_XCODE_DEVELOPER_DIR is required"),
+    );
@@ -75,0 +81,5 @@ fn normalizeArchive(
+    run_step.setEnvironmentVariable(
+        "DEVELOPER_DIR",
+        std.posix.getenv("MOCHA_XCODE_DEVELOPER_DIR") orelse
+            @panic("MOCHA_XCODE_DEVELOPER_DIR is required"),
+    );
diff --git a/src/build/LipoStep.zig b/src/build/LipoStep.zig
index c9c1530ef..4500b95ad 100644
--- a/src/build/LipoStep.zig
+++ b/src/build/LipoStep.zig
@@ -31,0 +32,5 @@ pub fn create(b: *std.Build, opts: Options) *LipoStep {
+    run_step.setEnvironmentVariable(
+        "DEVELOPER_DIR",
+        std.posix.getenv("MOCHA_XCODE_DEVELOPER_DIR") orelse
+            @panic("MOCHA_XCODE_DEVELOPER_DIR is required"),
+    );
diff --git a/src/build/MetallibStep.zig b/src/build/MetallibStep.zig
index fcf3055f8..14d7e01da 100644
--- a/src/build/MetallibStep.zig
+++ b/src/build/MetallibStep.zig
@@ -58,0 +59,5 @@ pub fn create(b: *std.Build, opts: Options) ?*MetallibStep {
+    run_ir.setEnvironmentVariable(
+        "DEVELOPER_DIR",
+        std.posix.getenv("MOCHA_XCODE_DEVELOPER_DIR") orelse
+            @panic("MOCHA_XCODE_DEVELOPER_DIR is required"),
+    );
@@ -73,0 +79,5 @@ pub fn create(b: *std.Build, opts: Options) ?*MetallibStep {
+    run_lib.setEnvironmentVariable(
+        "DEVELOPER_DIR",
+        std.posix.getenv("MOCHA_XCODE_DEVELOPER_DIR") orelse
+            @panic("MOCHA_XCODE_DEVELOPER_DIR is required"),
+    );
diff --git a/src/build/XCFrameworkStep.zig b/src/build/XCFrameworkStep.zig
index 39f0f9bac..a10676836 100644
--- a/src/build/XCFrameworkStep.zig
+++ b/src/build/XCFrameworkStep.zig
@@ -52,0 +53,5 @@ pub fn create(b: *std.Build, opts: Options) *XCFrameworkStep {
+        run.setEnvironmentVariable(
+            "DEVELOPER_DIR",
+            std.posix.getenv("MOCHA_XCODE_DEVELOPER_DIR") orelse
+                @panic("MOCHA_XCODE_DEVELOPER_DIR is required"),
+        );
diff --git a/src/termio/backend.zig b/src/termio/backend.zig
index 4fe3fb4ac..37550596c 100644
--- a/src/termio/backend.zig
+++ b/src/termio/backend.zig
@@ -127,0 +128 @@ pub const Backend = union(Kind) {
+            .callback => null,
